# dbt sources: duckdb database

version: 2

sources:
  - name: main
    schema: main
    tables:
      - name: python_asset
        description: "This table is generated by a python dagster asset that sits in the middle of the dbt DAG"
        meta:
          dagster:
            asset_key: ["python_asset"]